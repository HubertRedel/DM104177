
--Aby uruchomić projekt należy mieć zainstalowanego dockera i wpisać poniższe komendy--

docker build --tag gender-recognition-app
docker run --name python-app -p 5000:5000 gender-recognition-app

Flask:
localhost:5000